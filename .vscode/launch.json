{
  "configurations": [
    {
      "args": ["${file}"],
      "cwd": "${workspaceFolder}",
      "name": "Launch Program",
      "request": "launch",
      "runtimeExecutable": "node",
      "type": "node"
    }
  ],
  "version": "0.2.0"
}